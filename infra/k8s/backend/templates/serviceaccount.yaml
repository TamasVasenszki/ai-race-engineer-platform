apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: ai-race-backend
# No eks.amazonaws.com/role-arn annotation: Pod Identity binds the IAM role to this
# (namespace, serviceAccount) through the Terraform aws_eks_pod_identity_association.
